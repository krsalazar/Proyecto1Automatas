#include <stdio.h>
#include <stdlib.h>
#include <io.h>
void main()
{
 int i,j,k;
 char n[10];

 printf(“Ingrese numero:”);
 gets(n);
 i = atoi(n);
 j = 1:
 /* … Calcula el Factorial … */
 for(k=1;k<=i;k++)
 j = j*k;
 printf(“El Factorial es: %s\n”,j);
}
